import { SearchFruitPipe } from './search-fruit.pipe';

describe('SearchFruitPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFruitPipe();
    expect(pipe).toBeTruthy();
  });
});
